#include <iostream>
using namespace std;

int main()
 {
  int number; 
  int digit; 
  int sum=0;

std::cout << "What is your number!\n";
cin >> number; 
cout << "Your number is: "  << number << endl;
cout << endl << endl;

while (number>0)
{
  digit = number%10;
sum	= (sum*10) + digit;
number = number/10;
}
cout << "IDK WHAT THIS DO: " <<digit << endl;
cout <<"sum: "<<sum <<endl;

return 0;
/*long int num,sum;
	int digit;
	
	//input integer number
	cout<<"Enter an integer number: ";
	cin>>num;
	
	//check validation
	if(num<0)
	{
		cout<<"Input positive number!!!"<<endl;
		return -1;
	}
	
	//Reverse number
	sum=0;
	while(num>0)
	{
		digit=num%10;	//get digit
		sum	= (sum*10) + digit;
		num=num/10;
	}
	
	cout<<"Reverse number is: "<<sum<<endl;
	
	return 0;	

*/
}

